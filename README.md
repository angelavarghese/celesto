# 🪐 Celesto: AI Habitability Lab

A Multi-Agent AI system that analyzes exoplanet data to determine habitability potential. Features a Streamlit dashboard for searching real NASA data or designing custom planets.

## 🛠️ Installation

1.  **Create a virtual environment (optional but recommended):**
    ```bash
    python -m venv venv
    # Windows:
    .\venv\Scripts\activate
    # Mac/Linux:
    source venv/bin/activate
    ```

2.  **Install dependencies:**
    ```bash
    pip install -r requirements.txt
    ```

## 🚀 Usage

Run the interactive dashboard:

```bash
streamlit run app.py
```